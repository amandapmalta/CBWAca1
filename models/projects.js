const db = require('../db')();

module.export () => {
    
    const get = async (slug = null) => {
        if (!slug){
            const 
        }
     
     return slug;
    }
const add = (slug, name, description) => {
    const results = 
    }
    return {
     get,
     add
    }
};
    